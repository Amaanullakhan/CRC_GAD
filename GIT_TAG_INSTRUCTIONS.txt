Pin this commit after pushing to GitHub:

  cd CRC_GAD
  # Windows (PowerShell) — or run:  .\scripts\push_to_github.ps1
  git init
  git add .
  git commit -m "Rebuild CRC-GAD v1.0-rebuild — reproducible experiments"
  git branch -M main
  git remote add origin https://github.com/Amaanullakhan/CRC_GAD.git
  git push -u origin main
  git tag v1.0-rebuild
  git push origin v1.0-rebuild

Reference in paper: \url{https://github.com/Amaanullakhan/CRC_GAD/tree/v1.0-rebuild}

Before tagging (already done locally):
  python scripts/dedupe_results.py
  python scripts/generate_paper_tables.py
  python scripts/regenerate_figures.py

Current results: results/main_results.csv (30 rows = 6 datasets x 5 seeds)

NOTE: Git was not installed on the build machine. Install from https://git-scm.com/download/win
then run scripts/push_to_github.ps1
